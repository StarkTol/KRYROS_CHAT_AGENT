# Push KRYROS CHAT AGENT to GitHub

Run these commands in your terminal```bash
#:

 Navigate to the project folder
cd c:/Users/AsaphisPC/Bussiness_Support

# Initialize git (if not already initialized)
git init

# Add all files
git add .

# Commit with message
git commit -m "KRYROS CHAT AGENT - AI WhatsApp Chatbot with Human Takeover"

# Rename branch to main
git branch -M main

# Add remote (your repository)
git remote add origin https://github.com/StarkTol/KRYROS_CHAT_AGENT.git

# Push to GitHub
git push -u origin main
```

## If repository already exists:

```bash
cd c:/Users/AsaphisPC/Bussiness_Support
git add .
git commit -m "Update: KRYROS CHAT AGENT"
git push origin main
```

## Files included:
- ✅ Backend (NestJS + Prisma + PostgreSQL)
- ✅ Frontend (Next.js + Tailwind)
- ✅ AI Chatbot with GPT-4
- ✅ Human Takeover detection
- ✅ WhatsApp/Instagram/Facebook integration
- ✅ Complete documentation
